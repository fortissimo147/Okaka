# スコープ設計書: アクティブETF資産変動モニタリング

## 1. 概要

| 項目 | 内容 |
|---|---|
| 課題 | 特定のアクティブETFファンドの資産変動内訳をモニタリングしたい |
| 解決策 | 営業日の朝8時に資産内訳ファイルを自動ダウンロードしてSQLiteに蓄積し、静的Webダッシュボードでグラフとしてモニタリングできるようにする |
| 対象ユーザー | 特定アクティブETFの保有者・ウォッチャー |
| アーキテクチャ | Pythonスクリプト（収集・DB蓄積・JSON生成）+ 静的HTML（Chart.jsでグラフ表示）。バックエンドサーバー不要 |

## 2. 入力データ

| データ | 内容 |
|---|---|
| 資産内訳ファイル（CSV） | `https://inav.ice.com/pcf-download/all/all_pcf_YYYYMMDD.zip` からダウンロード。ZIP内に全ETFのCSVが入っており、`2083tsepcf_*.csv` を取り出して使用。**日付はファイル名でなくファイル内の `Fund Date` フィールドから取得する** |
### ファイル配置

過去ファイルも含め、すべて `data/raw/` に置く。ファイル名はそのままでOK（`2083.csv`, `2083 (2).csv` ...）。取り込みスクリプトがファイルを開いて `fund date` を読み、日付ベースでDBに格納する。

## 3. 機能一覧

### MVP（必ず作る）

| # | 機能 | 説明 |
|---|---|---|
| 1 | 自動ダウンロード＆蓄積 | cronで毎朝8時起動。営業日リストCSVを参照して当日が営業日なら資産内訳ファイルをダウンロードしSQLiteに格納 |
| 2 | 銘柄変動モニタリング | 前日比で新規追加・処分・買い増し・削減になった銘柄を検出しダッシュボードに表示 |
| 3 | 保有割合の時系列モニタリング | 個別銘柄のポートフォリオ比率の推移をグラフで確認できる |
| 4 | Webダッシュボード | 上記2・3をブラウザでグラフとして確認できる静的HTML |

### ストレッチ（時間があれば）

| # | 機能 | 説明 |
|---|---|---|
| 5 | 複数ファンド対応 | ファンドをパラメータ化して複数ファンドの切り替え・比較ができる |

## 4. 画面構成

1画面構成。以下のセクションを上から並べる。

```
[ファンド名・最終更新日時]

[銘柄変動サマリ（当日 or 選択日）]
  - 新規追加: 銘柄A, 銘柄B
  - 処分:     銘柄C
  - 買い増し: 銘柄D (+2.1%)
  - 削減:     銘柄E (-1.3%)

[保有割合 上位N銘柄の時系列折れ線グラフ（Chart.js）]
  横軸: 日付 / 縦軸: ポートフォリオ比率(%)

[全銘柄テーブル（当日スナップショット）]
  銘柄コード / 銘柄名 / 保有比率 / 前日比
```

## 5. 技術スタック

| レイヤー | 技術 | 選定理由 |
|---|---|---|
| 言語 | Python 3.10+ | CSV/Excel処理・SQLite操作・HTTP取得が簡潔 |
| HTTPクライアント | requests | シンプルなダウンロードに十分。JSレンダリングが必要な場合はplaywright |
| データ処理 | pandas | CSV/Excelのパースと差分計算が簡潔 |
| DB | SQLite | セットアップ不要、ファイル1つで完結 |
| スケジューリング | cron | 営業日リストCSVを参照して毎朝8時に実行 |
| フロントエンド | HTML + JavaScript | ビルド不要、ブラウザで即確認 |
| グラフ描画 | Chart.js（CDN読み込み） | 導入が簡単 |
| パッケージ管理 | uv | 高速 |

## 6. ディレクトリ構成

```
projects/kimura/
├── input.md
├── idea.md
├── scope.md
├── pyproject.toml
├── data/
│   ├── business_days.csv        # 営業日リスト（事前整備）
│   └── raw/                     # ダウンロードした生ファイルを日付付きで保存
│       └── 2025-04-25.csv
├── db/
│   └── etf.db                   # SQLite
├── scripts/
│   ├── import_all.py            # 過去ファイルの一括取り込み（初回のみ）
│   ├── fetch.py                 # ダウンロード＆DB蓄積（cronから呼ぶ）
│   └── build_dashboard.py       # DB→JSON生成→静的HTML更新
└── app/
    ├── index.html
    ├── app.js
    └── data/
        └── dashboard.json       # ダッシュボード用に整形済みのJSON
```

## 7. 実装ロジック

### import_all.py（過去ファイルの一括取り込み・初回のみ実行）

1. `data/raw/` 内の全CSVファイルを走査
2. 各ファイルを開いて `fund date` フィールドから日付を取得
3. すでにDBに同じ日付のデータがあればスキップ
4. pandasでパース・正規化してSQLiteに挿入

### fetch.py（毎朝cronから実行）

1. `https://inav.ice.com/pcf-download/listOfZips` で最新の利用可能日付を取得
2. 最新日付が当日と一致しなければ即終了
3. `https://inav.ice.com/pcf-download/all/all_pcf_YYYYMMDD.zip` をダウンロード
4. ZIP内の `2083tsepcf_*.csv` を取り出して `data/raw/` に保存
5. ファイルを開いて `Fund Date` フィールドから日付を取得
6. pandasでパース（銘柄コード・銘柄名・保有比率などを正規化）
7. SQLiteの `holdings` テーブルに `(date, ticker, name, ratio, shares, value)` を挿入

### build_dashboard.py（fetch.py完了後に呼ぶ、またはcronで続けて実行）

1. SQLiteから直近N日分のデータを取得
2. 日付ごとに前日比を計算し、変動分類（新規/処分/買い増し/削減）を付与
3. 上位銘柄の時系列データをJSON化
4. `app/data/dashboard.json` に書き出し

### DBスキーマ

```sql
CREATE TABLE holdings (
    date     TEXT NOT NULL,
    ticker   TEXT NOT NULL,
    name     TEXT NOT NULL,
    ratio    REAL,   -- ポートフォリオ比率(%)
    shares   REAL,   -- 保有株数
    value    REAL,   -- 時価
    PRIMARY KEY (date, ticker)
);
```

## 8. 実装計画（2コマ = 180分）

Claude Code前提。「一気に生成 → 動かして対話でチューニング」のサイクルで進める。

### 1コマ目（90分）: MVPが動く状態まで

- **初期一括生成（0〜15分）**: scope.mdをClaude Codeに渡して、fetch.py・build_dashboard.py・DBスキーマ・HTML/JSのドラフトを一気に生成
- **動作確認（15〜30分）**: 手動でfetch.pyを実行してDB蓄積確認、index.htmlをブラウザで開いてグラフ表示確認
- **対話チューニング（30〜90分）**: 差分検出ロジックの精度、グラフの見た目、テーブルのソート・フィルタなどを対話で詰める（1サイクル10〜20分 × 3〜4本）

**目標**: 1コマ目終了時点でMVP 4機能（自動取得・銘柄変動・保有割合グラフ・ダッシュボード）が動く状態

### 2コマ目（90分）: 仕上げ＋ストレッチ

- **MVP仕上げ（0〜30分）**: cronの設定確認、エラー時のログ出力、空データ時の表示などを対話で詰める
- **ストレッチ（30〜90分）**: 複数ファンド対応（ファンドIDをパラメータ化、ダッシュボードに切替UI追加）を対話サイクルで実装

## 9. スコープ外

- 認証・ログイン機能
- リアルタイム更新（朝の1回取得のみ）
- スマホ最適化
- 外部公開・デプロイ（ローカル動作のみ）
- 株価・時価の自動取得（保有比率のみ扱う）
- プッシュ通知・アラート
