[project]
name = "kimura"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "curl-cffi>=0.15.0",
    "pandas",
    "requests",
    "xlrd>=2.0.2",
    "yfinance>=1.3.0",
]

[project.scripts]
import-all = "scripts.import_all:main"
fetch = "scripts.fetch:main"
build-dashboard = "scripts.build_dashboard:main"
